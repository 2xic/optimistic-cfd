describe('Factory', () => {
  it.skip('should be possible to update the smart contract and preserve state', () => {});

  it.skip('should be possible to create a new cfd token for asset supported by ChainLink', () => {
    /* 
        Basically we have a factory assets which creates the tokens with a associated pool,
        This way it's easy to add support for a new asset, and it will make more code reusable.
    */
  });
});
